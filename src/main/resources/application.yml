server:
  port: 8090

spring:
  application:
    name: proyecto1
  jpa:
    show-sql: true
    hibernate:
      dialect: MySQL8Dialect
      ddl-auto: create
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    dbname: mingeso1
    url: jdbc:mysql://localhost:3306/mingeso1
    username: root
    password: admin
